import 'package:flutter/material.dart';

class DurationVolumeGridView extends StatelessWidget {
  const DurationVolumeGridView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Gridview");
  }
}
