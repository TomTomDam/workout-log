import 'package:flutter/material.dart';

class InstructionsPane extends StatelessWidget {
  const InstructionsPane({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Instructions"));
  }
}
